<h1>
    <%= @user.user_name %>
</h1>

Name:
<%= @user.name %><br>
Email:
<%= @user.email %><br>
Title:
<%= @post.title %><br>
Body:
<%= @post.body %>

<hr>

<% end %>

<%= link_to "Edit User", edit_user_path(@user) if @user == current_user %><br>
<%= link_to "User's List", users_path %><br>
<%= link_to "Delete User", user_path, method: :delete, data: { confirm: "Are you sure you want to delete user?"} if @user == current_user %><br>
<%= link_to "New Post", new_user_post_path(@user) if @user == current_user %>
