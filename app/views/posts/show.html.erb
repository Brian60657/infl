<h1>Posts</h1>

Establishment:
<%= @post.title %><br>

Line Length (None, Small, Large):
<%= @post.body %><br>

<hr>

<%= link_to "Edit Post", edit_post_path(@post) if @user == current_user %>
<%= link_to "Delete Post", post_path, method: :delete, data: { confirm: "Are you sure you to delete this post?"} if @user == current_user %>

<% end %>
